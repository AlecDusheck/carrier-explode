# tools

- [ftab/](ftab/) - parser for Apple C1/C1X modem firmware (`ftab.bin`: HWID table, LZFSE board blobs, code-image segments), in stdlib Python. Carrier `.der.pri`/`.der.gri` files are decoded by `src/lib/decode/pri.ts`.
